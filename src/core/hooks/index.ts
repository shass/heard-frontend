// Platform detection and configuration
export { usePlatform, usePlatformOrThrow, PlatformProvider } from './usePlatform'

// Authentication strategy
export { useAuth } from './useAuth'

// Wallet strategy
export { useWallet } from './useWallet'
